#!/usr/bin/env bash
# Repair a recognised score's header without installing the package.
set -euo pipefail
repo="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo"
exec python3 -m bandparts.headercli "$@"
