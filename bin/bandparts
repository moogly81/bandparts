#!/usr/bin/env bash
# Run the CLI from anywhere inside the repo without installing it.
set -euo pipefail
repo="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo"
exec python3 -m bandparts.cli "$@"
