#!/usr/bin/env bash
# Run the MusicXML checks from anywhere inside the repo without installing.
set -euo pipefail
repo="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo"
exec python3 -m bandparts.checkcli "$@"
